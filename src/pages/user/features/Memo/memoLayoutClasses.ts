/** Masonry-style note grid: single column on phones, more columns as width grows. */
export const memoNoteGridClass =
  'columns-1 gap-2 sm:columns-2 sm:gap-3 xl:columns-3';

/** Single-column list: centered, Keep-style readable width. */
export const memoNoteListClass = 'mx-auto flex w-full max-w-[600px] flex-col gap-2';
