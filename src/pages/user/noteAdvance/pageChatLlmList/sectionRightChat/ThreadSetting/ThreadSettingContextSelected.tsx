const ThreadSettingContextSelected = ({ threadId }: { threadId: string }) => {
    return (
        <div>
            <h1>Thread Setting Context Selected</h1>
            <h1>{threadId}</h1>
        </div>
    )
}

export default ThreadSettingContextSelected;