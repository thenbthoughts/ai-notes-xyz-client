export interface tsSchemaAiModelListGroq {
    // identification
    id: string;

    // fields
    object: string;
    created: number;
    owned_by: string;
    active: boolean;
    context_window: number;
};